<h1>Burgers!</h1>

<h2>Available Burgers</h2>

<ul>
  {{#each burgers}}
    {{#unless isDevoured}}
      {{> burgers/burger-block isDevoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that have been devoured!</h2>

<ul>
  {{#each burgers}}
    {{#if isDevoured}}
      {{> burgers/burger-block isDevoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="isdevoured">Is Devoured?</label><br>
    <input type="radio" name="burgerstate" value="1" checked> Devoured!<br>
    <input type="radio" name="burgerstate" value="0"> Available
  </div>

  <button type="submit">Add Burger</button>
</form>